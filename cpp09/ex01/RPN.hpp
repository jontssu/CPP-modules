#ifndef RPN_HPP
# define RPN_HPP

#include <iostream>
#include <string>
#include <stack>
#include <sstream>

class Rpn {
	public:
		// Rpn();
		Rpn(std::string const &str);
		// Rpn(Rpn const &other);
		// Rpn& operator=(Rpn const &other);
		// ~Rpn();

	private:
};

#endif